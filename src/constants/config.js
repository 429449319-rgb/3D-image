// API 配置
// 后端服务器地址（通过代理访问，解决跨域问题）
export const API_BASE_URL = '/api';

// 文件服务器地址（需要替换 127.0.0.1 为同事的IP）
export const FILE_SERVER_IP = '10.213.19.130';

// 导航菜单项
export const NAV_ITEMS = ['首页', '三维物体', '机器人', '仿真场景库', '软件下载', '品牌入驻', '联系我们'];

// 分页配置
export const PAGE_SIZE_OPTIONS = [16, 32, 64];
export const DEFAULT_PAGE_SIZE = 16;
